1. Action handlings:
    - current Position should be highlighted
        - render strategy table for every player after each action?
        - render only the table of current player?
    - only viable actions should be an option (not more than stacksize should be playable)

2. json or csv files of most positions plus EV